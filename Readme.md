## Backend for Japanese Language Learning app.

### Start working by :

1. Clone the repo.
2. Install the dependencies `npm install`.
3. Start the server `npm run start`.

**warning:** Add `.env` file before starting the server !
